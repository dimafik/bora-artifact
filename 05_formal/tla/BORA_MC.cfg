\* Small finite model for re-checking the augmented spec, now including the
\* explicit Leader Append-Only transition property added for the camera-ready.
SPECIFICATION Spec

CONSTANTS
    N_MC = 5
    F_MC = 2
    MaxTerm_MC = 2
    Values_MC = {v1, v2}
    N = 5
    F = 2
    MaxTerm = 2
    Values = {v1, v2}

CONSTRAINT StateConstraint

INVARIANT Safety
PROPERTY LeaderAppendOnly
