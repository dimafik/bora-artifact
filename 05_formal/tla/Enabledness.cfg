\* Main run: the two enabledness facts Liveness.tla proves by ExpandENABLED,
\* re-checked here as TLC invariants over the same bounded model the paper
\* reports elsewhere (N=5, F=2, MaxTerm=3).  They are not axioms: Liveness.tla
\* discharges 311/311 obligations and declares no AXIOM.
SPECIFICATION Spec

CONSTANTS
    N = 5
    F = 2
    MaxTerm = 3
    Values = {v1, v2}

CONSTRAINT StateConstraint

INVARIANTS
    ElectEnabledInv
    CampEnabledInv

CHECK_DEADLOCK FALSE
