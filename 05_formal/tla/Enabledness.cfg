\* Main run: the two Liveness.tla axioms, checked as invariants over the same
\* bounded model the paper reports elsewhere (N=5, F=2, MaxTerm=3).
SPECIFICATION Spec

CONSTANTS
    N = 5
    F = 2
    MaxTerm = 3
    Values = {v1, v2}

CONSTRAINT StateConstraint

INVARIANTS
    ElectEnabledInv
    CampEnabledInv

CHECK_DEADLOCK FALSE
