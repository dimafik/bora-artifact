\* TLC configuration for BORA.tla
\* Model: N=5 orderers, F=2 fault bound, MaxTerm=3, Values={"v1","v2"}.
\* This is the canonical Fabric ordering-service configuration.

CONSTANTS
    N = 5
    F = 2
    MaxTerm = 3
    Values = {"v1", "v2"}

SPECIFICATION Spec

INVARIANTS
    TypeOK
    BoundedCap
    ALR_Inv
    ElectionSafety
    LogMatching
    StateMachineSafety
    LeaderCompleteness

CHECK_DEADLOCK FALSE
