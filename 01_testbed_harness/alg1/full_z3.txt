(* created new ".tlacache/Liveness.tlaps/Liveness.thy" *)
(* fingerprints written in ".tlacache/Liveness.tlaps/fingerprints" *)
File "./Liveness.tla", line 175, characters 3-4:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  Orderers == 1..N,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  TypeOK ==
                    /\ currentTerm \in [Orderers -> 0..MaxTerm]
                    /\ state
                       \in [Orderers -> {"follower", "candidate", "leader"}]
                    /\ votedFor \in [Orderers -> 0..N]
                    /\ log
                       \in [Orderers ->
                              Seq([term : 0..MaxTerm, value : Values])]
                    /\ commitIndex \in [Orderers -> Nat]
                    /\ leader \in [0..MaxTerm -> 0..N]
                    /\ history \in Seq([index : Nat, value : Values]),
                  Init ==
                    /\ currentTerm = [i \in Orderers |-> 0]
                    /\ state = [i \in Orderers |-> "follower"]
                    /\ votedFor = [i \in Orderers |-> 0]
                    /\ log = [i \in Orderers |-> <<>>]
                    /\ commitIndex = [i \in Orderers |-> 0]
                    /\ leader = [t \in 0..MaxTerm |-> 0]
                    /\ history = <<>>,
                  HasLeader == \E i \in Orderers : state[i] = "leader",
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  LeaderInv ==
                    \A t \in 0..MaxTerm :
                       leader[t] # 0 => state[leader[t]] = "leader",
                  FollowerT0 ==
                    \A i \in Orderers :
                       state[i] = "follower" => currentTerm[i] = 0,
                  Inv == TypeOK /\ LeaderInv /\ FollowerT0,
                  NAssume ,
                  MTAssume ,
                  NAssume ,
                  MTAssume 
           PROVE  Init => Inv /\ ~HasLeader
File "./Liveness.tla", line 164, characters 7-8:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  Orderers == 1..N,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  TypeOK ==
                    /\ currentTerm \in [Orderers -> 0..MaxTerm]
                    /\ state
                       \in [Orderers -> {"follower", "candidate", "leader"}]
                    /\ votedFor \in [Orderers -> 0..N]
                    /\ log
                       \in [Orderers ->
                              Seq([term : 0..MaxTerm, value : Values])]
                    /\ commitIndex \in [Orderers -> Nat]
                    /\ leader \in [0..MaxTerm -> 0..N]
                    /\ history \in Seq([index : Nat, value : Values]),
                  Campaign(i) ==
                    /\ state[i] = "follower"
                    /\ currentTerm[i] < MaxTerm
                    /\ currentTerm'
                       = [currentTerm EXCEPT !
                                   [i] = currentTerm[i] + 1]
                    /\ state' = [state EXCEPT !
                                   [i] = "candidate"]
                    /\ votedFor' = [votedFor EXCEPT !
                                   [i] = i]
                    /\ UNCHANGED <<log, commitIndex, leader, history>>,
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  Inv == TypeOK /\ LeaderInv /\ FollowerT0,
                  NAssume ,
                  MTAssume ,
                  Q == Inv /\ ~HasLeader /\ ~PendingCand,
                  NEW CONSTANT j \in Orderers,
                  <3>2 
           PROVE  (state')[j] = "candidate"
                  /\ (currentTerm')[j] = currentTerm[j] + 1
                  /\ leader' = leader
File "./Liveness.tla", line 162, characters 46-47:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  Orderers == 1..N,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  NAssume ,
                  MTAssume ,
                  Q == Inv /\ ~HasLeader /\ ~PendingCand,
                  NEW CONSTANT j \in Orderers,
                  <3>3 
           PROVE  currentTerm[j] + 1 \in 0..MaxTerm
File "./Liveness.tla", line 161, characters 7-8:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  Orderers == 1..N,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  TypeOK ==
                    /\ currentTerm \in [Orderers -> 0..MaxTerm]
                    /\ state
                       \in [Orderers -> {"follower", "candidate", "leader"}]
                    /\ votedFor \in [Orderers -> 0..N]
                    /\ log
                       \in [Orderers ->
                              Seq([term : 0..MaxTerm, value : Values])]
                    /\ commitIndex \in [Orderers -> Nat]
                    /\ leader \in [0..MaxTerm -> 0..N]
                    /\ history \in Seq([index : Nat, value : Values]),
                  Campaign(i) ==
                    /\ state[i] = "follower"
                    /\ currentTerm[i] < MaxTerm
                    /\ currentTerm'
                       = [currentTerm EXCEPT !
                                   [i] = currentTerm[i] + 1]
                    /\ state' = [state EXCEPT !
                                   [i] = "candidate"]
                    /\ votedFor' = [votedFor EXCEPT !
                                   [i] = i]
                    /\ UNCHANGED <<log, commitIndex, leader, history>>,
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  Inv == TypeOK /\ LeaderInv /\ FollowerT0,
                  NAssume ,
                  MTAssume ,
                  Q == Inv /\ ~HasLeader /\ ~PendingCand,
                  NEW CONSTANT j \in Orderers,
                  <3>2 
           PROVE  currentTerm[j] \in 0..MaxTerm /\ currentTerm[j] < MaxTerm
File "./Liveness.tla", line 158, characters 48-49:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  Orderers == 1..N,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  NAssume ,
                  MTAssume ,
                  Q == Inv /\ ~HasLeader /\ ~PendingCand,
                  NoLeaderZero 
           PROVE  \A t \in 0..MaxTerm : leader[t] = 0
File "./Liveness.tla", line 142, characters 48-49:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  Orderers == 1..N,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  NAssume ,
                  MTAssume ,
                  Q == Inv /\ ~HasLeader /\ ~PendingCand,
                  Q ,
                  [Next]_vars ,
                  NEW CONSTANT j \in Orderers,
                  <4>2 
           PROVE  currentTerm[j] + 1 \in 0..MaxTerm
File "./Liveness.tla", line 125, characters 32-33:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  Orderers == 1..N,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  TypeOK ==
                    /\ currentTerm \in [Orderers -> 0..MaxTerm]
                    /\ state
                       \in [Orderers -> {"follower", "candidate", "leader"}]
                    /\ votedFor \in [Orderers -> 0..N]
                    /\ log
                       \in [Orderers ->
                              Seq([term : 0..MaxTerm, value : Values])]
                    /\ commitIndex \in [Orderers -> Nat]
                    /\ leader \in [0..MaxTerm -> 0..N]
                    /\ history \in Seq([index : Nat, value : Values]),
                  BecomeLeader(i) ==
                    /\ state[i] = "candidate"
                    /\ leader[currentTerm[i]] = 0
                    /\ state' = [state EXCEPT !
                                   [i] = "leader"]
                    /\ leader' = [leader EXCEPT !
                                   [currentTerm[i]] = i]
                    /\ UNCHANGED <<currentTerm, votedFor, log, commitIndex,
                                   history>>,
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  Inv == TypeOK /\ LeaderInv /\ FollowerT0,
                  NAssume ,
                  MTAssume ,
                  P == Inv /\ PendingCand /\ ~HasLeader,
                  NEW CONSTANT j \in Orderers,
                  <3>2 
           PROVE  (state')[j] = "leader"
File "./Liveness.tla", line 108, characters 9-10:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  Orderers == 1..N,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  TypeOK ==
                    /\ currentTerm \in [Orderers -> 0..MaxTerm]
                    /\ state
                       \in [Orderers -> {"follower", "candidate", "leader"}]
                    /\ votedFor \in [Orderers -> 0..N]
                    /\ log
                       \in [Orderers ->
                              Seq([term : 0..MaxTerm, value : Values])]
                    /\ commitIndex \in [Orderers -> Nat]
                    /\ leader \in [0..MaxTerm -> 0..N]
                    /\ history \in Seq([index : Nat, value : Values]),
                  Campaign(i) ==
                    /\ state[i] = "follower"
                    /\ currentTerm[i] < MaxTerm
                    /\ currentTerm'
                       = [currentTerm EXCEPT !
                                   [i] = currentTerm[i] + 1]
                    /\ state' = [state EXCEPT !
                                   [i] = "candidate"]
                    /\ votedFor' = [votedFor EXCEPT !
                                   [i] = i]
                    /\ UNCHANGED <<log, commitIndex, leader, history>>,
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  Inv == TypeOK /\ LeaderInv /\ FollowerT0,
                  NAssume ,
                  MTAssume ,
                  P == Inv /\ PendingCand /\ ~HasLeader,
                  P ,
                  [Next]_vars ,
                  NEW CONSTANT i \in Orderers,
                  NEW CONSTANT j \in Orderers,
                  <4>1 ,
                  <4>3 
           PROVE  (state')[i] = "candidate"
                  /\ (currentTerm')[i] = currentTerm[i] /\ 
                  leader' = leader
File "./Liveness.tla", line 87, characters 5-6:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  vars ==
                    <<currentTerm, state, votedFor, log, commitIndex, leader,
                      history>>,
                  Campaign(i) ==
                    /\ state[i] = "follower"
                    /\ currentTerm[i] < MaxTerm
                    /\ currentTerm'
                       = [currentTerm EXCEPT !
                                   [i] = currentTerm[i] + 1]
                    /\ state' = [state EXCEPT !
                                   [i] = "candidate"]
                    /\ votedFor' = [votedFor EXCEPT !
                                   [i] = i]
                    /\ UNCHANGED <<log, commitIndex, leader, history>>,
                  CampStep == \E i \in Orderers : Campaign(i),
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  <1>3 ,
                  ExpandENABLED 
           PROVE  ENABLED <<CampStep>>_vars
File "./Liveness.tla", line 85, characters 5-6:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  Orderers == 1..N,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  vars ==
                    <<currentTerm, state, votedFor, log, commitIndex, leader,
                      history>>,
                  TypeOK ==
                    /\ currentTerm \in [Orderers -> 0..MaxTerm]
                    /\ state
                       \in [Orderers -> {"follower", "candidate", "leader"}]
                    /\ votedFor \in [Orderers -> 0..N]
                    /\ log
                       \in [Orderers ->
                              Seq([term : 0..MaxTerm, value : Values])]
                    /\ commitIndex \in [Orderers -> Nat]
                    /\ leader \in [0..MaxTerm -> 0..N]
                    /\ history \in Seq([index : Nat, value : Values]),
                  Campaign(i) ==
                    /\ state[i] = "follower"
                    /\ currentTerm[i] < MaxTerm
                    /\ currentTerm'
                       = [currentTerm EXCEPT !
                                   [i] = currentTerm[i] + 1]
                    /\ state' = [state EXCEPT !
                                   [i] = "candidate"]
                    /\ votedFor' = [votedFor EXCEPT !
                                   [i] = i]
                    /\ UNCHANGED <<log, commitIndex, leader, history>>,
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  Inv == TypeOK /\ LeaderInv /\ FollowerT0,
                  <1>3 ,
                  <1>4 ,
                  <1>5 ,
                  MTAssume ,
                  ExpandENABLED 
           PROVE  ENABLED <<Campaign(1)>>_vars
File "./Liveness.tla", line 83, characters 29-30:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  FollowerT0 ==
                    \A i \in Orderers :
                       state[i] = "follower" => currentTerm[i] = 0,
                  Inv == TypeOK /\ LeaderInv /\ FollowerT0,
                  <1>3 ,
                  <1>4 
           PROVE  currentTerm[1] = 0
File "./Liveness.tla", line 81, characters 35-36:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  PendingCand ==
                    \E i \in Orderers :
                       state[i] = "candidate" /\ leader[currentTerm[i]] = 0,
                  <1>3 ,
                  <1>2 ,
                  <2>3 
           PROVE  state[1] # "candidate"
File "./Liveness.tla", line 80, characters 42-43:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  TypeOK ==
                    /\ currentTerm \in [Orderers -> 0..MaxTerm]
                    /\ state
                       \in [Orderers -> {"follower", "candidate", "leader"}]
                    /\ votedFor \in [Orderers -> 0..N]
                    /\ log
                       \in [Orderers ->
                              Seq([term : 0..MaxTerm, value : Values])]
                    /\ commitIndex \in [Orderers -> Nat]
                    /\ leader \in [0..MaxTerm -> 0..N]
                    /\ history \in Seq([index : Nat, value : Values]),
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  Inv == TypeOK /\ LeaderInv /\ FollowerT0,
                  <1>3 
           PROVE  currentTerm[1] \in 0..MaxTerm
File "./Liveness.tla", line 79, characters 32-33:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  HasLeader == \E i \in Orderers : state[i] = "leader",
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  <1>3 
           PROVE  state[1] # "leader"
File "./Liveness.tla", line 78, characters 59-60:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  TypeOK ==
                    /\ currentTerm \in [Orderers -> 0..MaxTerm]
                    /\ state
                       \in [Orderers -> {"follower", "candidate", "leader"}]
                    /\ votedFor \in [Orderers -> 0..N]
                    /\ log
                       \in [Orderers ->
                              Seq([term : 0..MaxTerm, value : Values])]
                    /\ commitIndex \in [Orderers -> Nat]
                    /\ leader \in [0..MaxTerm -> 0..N]
                    /\ history \in Seq([index : Nat, value : Values]),
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  Inv == TypeOK /\ LeaderInv /\ FollowerT0,
                  <1>3 
           PROVE  state[1] \in {"follower", "candidate", "leader"}
File "./Liveness.tla", line 75, characters 46-47:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  NoLeaderZero 
           PROVE  \A t \in 0..MaxTerm : leader[t] = 0
File "./Liveness.tla", line 70, characters 5-6:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  vars ==
                    <<currentTerm, state, votedFor, log, commitIndex, leader,
                      history>>,
                  BecomeLeader(i) ==
                    /\ state[i] = "candidate"
                    /\ leader[currentTerm[i]] = 0
                    /\ state' = [state EXCEPT !
                                   [i] = "leader"]
                    /\ leader' = [leader EXCEPT !
                                   [currentTerm[i]] = i]
                    /\ UNCHANGED <<currentTerm, votedFor, log, commitIndex,
                                   history>>,
                  ElectStep == \E i \in Orderers : BecomeLeader(i),
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  NEW CONSTANT i \in Orderers,
                  <1>2 ,
                  ExpandENABLED 
           PROVE  ENABLED <<ElectStep>>_vars
File "./Liveness.tla", line 68, characters 5-6:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  Orderers == 1..N,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  vars ==
                    <<currentTerm, state, votedFor, log, commitIndex, leader,
                      history>>,
                  TypeOK ==
                    /\ currentTerm \in [Orderers -> 0..MaxTerm]
                    /\ state
                       \in [Orderers -> {"follower", "candidate", "leader"}]
                    /\ votedFor \in [Orderers -> 0..N]
                    /\ log
                       \in [Orderers ->
                              Seq([term : 0..MaxTerm, value : Values])]
                    /\ commitIndex \in [Orderers -> Nat]
                    /\ leader \in [0..MaxTerm -> 0..N]
                    /\ history \in Seq([index : Nat, value : Values]),
                  BecomeLeader(i) ==
                    /\ state[i] = "candidate"
                    /\ leader[currentTerm[i]] = 0
                    /\ state' = [state EXCEPT !
                                   [i] = "leader"]
                    /\ leader' = [leader EXCEPT !
                                   [currentTerm[i]] = i]
                    /\ UNCHANGED <<currentTerm, votedFor, log, commitIndex,
                                   history>>,
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  Inv == TypeOK /\ LeaderInv /\ FollowerT0,
                  NEW CONSTANT i \in Orderers,
                  <1>2 ,
                  NAssume ,
                  ExpandENABLED 
           PROVE  ENABLED <<BecomeLeader(i)>>_vars
File "./Liveness.tla", line 66, characters 5-6:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  PendingCand ==
                    \E i \in Orderers :
                       state[i] = "candidate" /\ leader[currentTerm[i]] = 0
           PROVE  \E i \in Orderers :
                     state[i] = "candidate" /\ leader[currentTerm[i]] = 0
File "./Liveness.tla", line 58, characters 39-40:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  LeaderInv ==
                    \A t \in 0..MaxTerm :
                       leader[t] # 0 => state[leader[t]] = "leader",
                  Inv == TypeOK /\ LeaderInv /\ FollowerT0,
                  NEW CONSTANT t \in 
                  0..MaxTerm
           PROVE  state[leader[t]] = "leader"
File "./Liveness.tla", line 59, characters 15-16:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  HasLeader == \E i \in Orderers : state[i] = "leader",
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  NEW CONSTANT t \in 
                  0..MaxTerm,
                  <2>1 ,
                  <2>2 
           PROVE  FALSE
File "./Liveness.tla", line 55, characters 28-29:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  Orderers == 1..N,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  TypeOK ==
                    /\ currentTerm \in [Orderers -> 0..MaxTerm]
                    /\ state
                       \in [Orderers -> {"follower", "candidate", "leader"}]
                    /\ votedFor \in [Orderers -> 0..N]
                    /\ log
                       \in [Orderers ->
                              Seq([term : 0..MaxTerm, value : Values])]
                    /\ commitIndex \in [Orderers -> Nat]
                    /\ leader \in [0..MaxTerm -> 0..N]
                    /\ history \in Seq([index : Nat, value : Values]),
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  Inv == TypeOK /\ LeaderInv /\ FollowerT0,
                  NEW CONSTANT t \in 
                  0..MaxTerm
           PROVE  leader[t] \in 0..N
File "./Liveness.tla", line 49, characters 22-23:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  Orderers == 1..N,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  TypeOK ==
                    /\ currentTerm \in [Orderers -> 0..MaxTerm]
                    /\ state
                       \in [Orderers -> {"follower", "candidate", "leader"}]
                    /\ votedFor \in [Orderers -> 0..N]
                    /\ log
                       \in [Orderers ->
                              Seq([term : 0..MaxTerm, value : Values])]
                    /\ commitIndex \in [Orderers -> Nat]
                    /\ leader \in [0..MaxTerm -> 0..N]
                    /\ history \in Seq([index : Nat, value : Values]),
                  Init ==
                    /\ currentTerm = [i \in Orderers |-> 0]
                    /\ state = [i \in Orderers |-> "follower"]
                    /\ votedFor = [i \in Orderers |-> 0]
                    /\ log = [i \in Orderers |-> <<>>]
                    /\ commitIndex = [i \in Orderers |-> 0]
                    /\ leader = [t \in 0..MaxTerm |-> 0]
                    /\ history = <<>>,
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  LeaderInv ==
                    \A t \in 0..MaxTerm :
                       leader[t] # 0 => state[leader[t]] = "leader",
                  FollowerT0 ==
                    \A i \in Orderers :
                       state[i] = "follower" => currentTerm[i] = 0,
                  Inv == TypeOK /\ LeaderInv /\ FollowerT0,
                  NAssume ,
                  MTAssume 
           PROVE  Init => Inv
File "./Liveness.tla", line 45, characters 45-46:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  Orderers == 1..N,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  TypeOK ==
                    /\ currentTerm \in [Orderers -> 0..MaxTerm]
                    /\ state
                       \in [Orderers -> {"follower", "candidate", "leader"}]
                    /\ votedFor \in [Orderers -> 0..N]
                    /\ log
                       \in [Orderers ->
                              Seq([term : 0..MaxTerm, value : Values])]
                    /\ commitIndex \in [Orderers -> Nat]
                    /\ leader \in [0..MaxTerm -> 0..N]
                    /\ history \in Seq([index : Nat, value : Values]),
                  Commit(i) ==
                    /\ state[i] = "leader"
                    /\ Len(log[i]) > commitIndex[i]
                    /\ commitIndex' = [commitIndex EXCEPT !
                                   [i] = Len(log[i])]
                    /\ history'
                       = Append(history,
                                [index |-> Len(log[i]),
                                 value |-> log[i][Len(log[i])].value])
                    /\ UNCHANGED <<currentTerm, state, votedFor, log,
                                   leader>>,
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  LeaderInv ==
                    \A t \in 0..MaxTerm :
                       leader[t] # 0 => state[leader[t]] = "leader",
                  FollowerT0 ==
                    \A i \in Orderers :
                       state[i] = "follower" => currentTerm[i] = 0,
                  Inv == TypeOK /\ LeaderInv /\ FollowerT0,
                  Inv ,
                  [Next]_vars ,
                  <1>5 
           PROVE  Inv'
File "./Liveness.tla", line 40, characters 19-20:
[ERROR]: Could not prove or check:
           ASSUME NEW CONSTANT N,
                  NEW CONSTANT F,
                  NEW CONSTANT MaxTerm,
                  NEW CONSTANT Values,
                  Orderers == 1..N,
                  NEW VARIABLE currentTerm,
                  NEW VARIABLE state,
                  NEW VARIABLE votedFor,
                  NEW VARIABLE log,
                  NEW VARIABLE commitIndex,
                  NEW VARIABLE leader,
                  NEW VARIABLE history,
                  TypeOK ==
                    /\ currentTerm \in [Orderers -> 0..MaxTerm]
                    /\ state
                       \in [Orderers -> {"follower", "candidate", "leader"}]
                    /\ votedFor \in [Orderers -> 0..N]
                    /\ log
                       \in [Orderers ->
                              Seq([term : 0..MaxTerm, value : Values])]
                    /\ commitIndex \in [Orderers -> Nat]
                    /\ leader \in [0..MaxTerm -> 0..N]
                    /\ history \in Seq([index : Nat, value : Values]),
                  BecomeLeader(i) ==
                    /\ state[i] = "candidate"
                    /\ leader[currentTerm[i]] = 0
                    /\ state' = [state EXCEPT !
                                   [i] = "leader"]
                    /\ leader' = [leader EXCEPT !
                                   [currentTerm[i]] = i]
                    /\ UNCHANGED <<currentTerm, votedFor, log, commitIndex,
                                   history>>,
                  NAssume == N >= 1,
                  MTAssume == MaxTerm >= 1,
                  LeaderInv ==
                    \A t \in 0..MaxTerm :
                       leader[t] # 0 => state[leader[t]] = "leader",
                  FollowerT0 ==
                    \A i \in Orderers :
                       state[i] = "follower" => currentTerm[i] = 0,
                  Inv == TypeOK /\ LeaderInv /\ FollowerT0,
                  Inv ,
                  [Next]_vars ,
                  NEW CONSTANT i \in Orderers,
                  <2>1 ,
                  NAssume 
           PROVE  TypeOK'
File "./Liveness.tla", line 1, character 1 to line 177, character 77:
[ERROR]: 25/258 obligations failed.
There were backend errors processing module `"Liveness"`.
 tlapm ending abnormally with Failure("backend errors: there are unproved obligations")
Raised at Stdlib.failwith in file "stdlib.ml", line 29, characters 17-33
Called from Tlapm_lib.process_module in file "src/tlapm_lib.ml", line 448, characters 12-77
Called from Tlapm_lib.main.f in file "src/tlapm_lib.ml", line 577, characters 23-43
Called from Stdlib__List.fold_left in file "list.ml", line 123, characters 24-34
Called from Tlapm_lib.main in file "src/tlapm_lib.ml", line 580, characters 13-40
Called from Tlapm_lib.init in file "src/tlapm_lib.ml", line 592, characters 8-59

exit=3
